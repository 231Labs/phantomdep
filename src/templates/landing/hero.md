# PhantomDep Gate

Block hallucinated dependencies before they install.

CI + install gate: registry resolve + allowlist. Engineering findings, not an SCA suite.

Registry / allowlist gate — engineering findings only. Not SCA, not malware-free, not “secure by default.”
